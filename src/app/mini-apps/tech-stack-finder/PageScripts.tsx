'use client'

/**
 * Aurora and HowItWorks effects are handled by shared components.
 * Keep this file as a stable hook for future page-only interactions.
 */
export function PageScripts() {
  return null
}
