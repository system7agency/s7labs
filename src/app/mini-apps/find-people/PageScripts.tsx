'use client'

/**
 * Per-page client effects.
 *
 * Background (aurora + cursor spotlight) is handled by <AuroraBackground />.
 * "How it works" fade-up is handled by <HowItWorks />. Kept as a no-op
 * placeholder so the page import keeps working.
 */
export function PageScripts() {
  return null
}
